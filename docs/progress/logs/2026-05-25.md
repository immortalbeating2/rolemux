# 2026-05-25 开发日志

## 背景

用户要求参考 Ubuntu2 下 `/home/peng8/project/Analysis/multi-domain-bidding-platform/AGENTS.md`，结合 RoleMux 项目需求生成自己的 `AGENTS.md`，并基于 subagents 生成对应 agents 设置，同时不要少了三个留痕文档。

用户随后要求使用 Superpowers 思路，将阶段开发文档和实施文档吸收到 `AGENTS.md`，并按照所有开发内容拆分阶段开发文档。

## 参考内容

已读取参考项目 `AGENTS.md`，吸收以下结构：

- 项目目的与快照。
- 规则优先级。
- 必须遵守的硬约束。
- 核心文档入口。
- Agent 行为准则。
- 工作流门禁。
- Session 接续顺序。
- 按任务类型继续读取。
- Subagent 协作与文件所有权。
- 文档留痕与时间线。
- 验证约定。

## 本次改动

- 新增 `AGENTS.md`，作为 RoleMux 项目级代理工作指南。
- 新增 `docs/progress/status.md`，作为当前真实状态入口。
- 新增 `docs/progress/timeline.md`，记录项目关键时间线。
- 新增 `docs/progress/logs/2026-05-25.md`，记录本次开发日志。
- 初始化 git 仓库，默认分支为 `main`。
- 新增 `.gitignore`，排除依赖、构建产物、运行日志、环境变量和 `.rolemux/tasks/` 运行产物。
- 新增 `.gitattributes`，固定文本文件 LF 行尾并将常见图片格式作为二进制处理，减少 Windows/macOS/Linux 协作时的行尾噪音。
- 新增 `docs/dev/code-style.md`，细化 TypeScript、模块边界、命名、注释和提交前检查。
- 更新 `AGENTS.md`，把代码规范、注释约定和 `.codex/agents/` 角色设置纳入项目级约束。
- 新增 `.codex/agents/`，为 researcher、architect、cli-builder、skill-builder、reviewer、docs-keeper 生成本地角色设置和 prompt 片段。
- 新增 `spec/phases/README.md` 和 M0-M6 阶段开发文档。
- 新增 `spec/implementation/README.md` 和 M0-M6 阶段实施文档。
- 更新 `AGENTS.md`，把阶段文档、实施文档、阶段读取顺序、阶段退出标准和任务类型读取规则吸收到项目级约束。
- 更新 `.codex/agents/agents.json` 和 docs-keeper 角色提示，使 subagent 也读取阶段文档与实施文档。
- 创建开发分支 `feature/complete-rolemux-plugin` 执行完整实现。
- 初始化 npm/TypeScript/Vitest/tsup 工程，新增 `package.json`、`tsconfig.json`、`vitest.config.ts`、`tsup.config.ts` 和 `package-lock.json`。
- 新增 `src/`，实现 CLI 命令、core 模块、provider adapter、task store、fallback、prompt builder 和 HTML report。
- 新增 `tests/`，覆盖 CLI smoke、命令 dry-run、provider adapter、process runner、task store、fallback、HTML report 和 install。
- 新增 `skills/`、`roles/`、`templates/`、`examples/`、`docs/release/` 和 `LICENSE`。
- 更新 `README.md`，补充安装、命令、Skill、任务产物、配置、示例、开发和安全默认值。

## 关键决策

- RoleMux 仓库自身使用 `AGENTS.md` 规范开发流程。
- RoleMux 产品安装到用户项目时默认不要求修改用户项目 `AGENTS.md`。
- `--with-agents` 只能作为显式可选能力。
- Subagent 设置采用角色模板方式写入 `AGENTS.md`，包括 researcher、architect、cli-builder、skill-builder、reviewer、docs-keeper。
- 三个留痕文档固定为：
  - `docs/progress/status.md`
  - `docs/progress/timeline.md`
  - `docs/progress/logs/YYYY-MM-DD.md`
- 用户要求的 `.codoex` 按 Codex 项目约定落地为 `.codex`，避免生成拼写错误目录。
- 注释策略采用“解释意图和边界，不逐行复述代码”：导出 API、provider adapter、跨平台路径、安全边界、task artifact schema、并发/fallback/超时等必须有注释。
- 阶段文档采用 `spec/phases/` 管理“做什么、交付什么、何时退出”。
- 实施文档采用 `spec/implementation/` 管理“改哪些文件、按什么步骤、运行什么验证、交给哪些 subagent”。

## 影响范围

- 早期 AGENTS/留痕初始化只影响项目文档和开发约束。
- M0-M6 实现阶段已新增源码、CLI 命令、provider adapter、安装器、测试、Skill、roles、examples 和 release docs。
- 不改变 `spec/rolemux-development-spec.md` 的产品基线。
- git 初始化影响版本管理方式，但不改变产品运行逻辑。
- `.gitattributes` 影响 git 中文本文件行尾规范化，不改变文件内容语义。
- `.codex/agents/` 是本仓库开发协作配置，不会作为 RoleMux 安装默认写入用户项目。
- `spec/phases/` 和 `spec/implementation/` 只影响后续开发组织方式，不改变产品默认安装行为。

## 验证记录

M0-M6 MVP 实现验证命令：

```powershell
npm run typecheck
npm test
npm run build
node .\dist\cli.js --help
node .\dist\cli.js doctor
node .\dist\cli.js install --dry-run
node .\dist\cli.js run --provider codex --role builder --task .\examples\basic-task.md --workdir . --dry-run
node .\dist\cli.js plan --providers codex,claude --task .\examples\basic-task.md --workdir . --dry-run
node .\dist\cli.js review --provider codex --task .\examples\basic-task.md --workdir . --dry-run
node .\dist\cli.js discuss --providers codex,claude,agy --task .\examples\basic-task.md --workdir . --mode parallel --dry-run
npm pack --dry-run
git diff --check
```

验证结果：

- `npm run typecheck` 通过。
- `npm test` 通过，11 个 test files / 17 个 tests。
- `npm run build` 通过，生成 `dist/cli.js`、`dist/cli.js.map`、`dist/cli.d.ts`。
- `node .\dist\cli.js --help`、`doctor`、`install --dry-run`、`run --dry-run`、`plan --dry-run`、`review --dry-run`、`discuss --dry-run` 均 exit 0。
- dry-run 后 `Test-Path .rolemux\tasks` 为 `False`，未创建任务产物。
- `npm pack --dry-run` 通过并触发构建，包清单包含 `dist`、`skills`、`roles`、`templates`、`examples`、`docs/release/checklist.md`、`README.md`、`LICENSE`。
- `git diff --check` 通过。
- 真实 install 已在临时 HOME 验证，生成 config、roles、Codex Skill、Claude Skill；未写真实用户 HOME。
- 最终 reviewer subagent 发现的 role prompt、M5 非 dry-run、review 示例、doctor requested providers、prepack、discuss mode 校验问题已处理；后续验证确认 role prompt 进入 dry-run prompt，`doctor --providers codex` 返回 `ok: true`，非法 discuss mode 返回非零。
- Windows 下 mock `.cmd` provider 验证不稳定，未执行真实 provider mock run；本轮以 provider adapter 参数数组测试、process runner 测试和 CLI dry-run 作为替代验证。

本轮新增验证命令：

```powershell
git status --short
git branch --show-current
Test-Path -LiteralPath '.git'
Test-Path -LiteralPath '.gitignore'
Test-Path -LiteralPath 'docs\dev\code-style.md'
Test-Path -LiteralPath '.codex\agents\agents.json'
Get-ChildItem -LiteralPath '.codex\agents' -File
Select-String -LiteralPath 'AGENTS.md' -Pattern 'docs/dev/code-style.md','.codex/agents/','导出的函数','provider 参数'
Select-String -LiteralPath 'docs\dev\code-style.md' -Pattern '必须写注释的场景','导出的函数','外部命令必须优先使用参数数组'
```

验证结果：

- 当前 git 分支当时为 `main`；MVP 实现阶段后续已切换到 `feature/complete-rolemux-plugin`。
- `.git`、`.gitignore`、`docs/dev/code-style.md`、`.codex/agents/agents.json`、`.codex/agents/rolemux-cli-builder.md`、`.codex/agents/rolemux-reviewer.md` 均存在。
- `.codex/agents/agents.json` 可被 PowerShell `ConvertFrom-Json` 解析。
- `agents.json` 包含 6 个角色：`rolemux-researcher`、`rolemux-architect`、`rolemux-cli-builder`、`rolemux-skill-builder`、`rolemux-reviewer`、`rolemux-docs-keeper`。
- `AGENTS.md` 已包含 `docs/dev/code-style.md`、`.codex/agents/`、导出 API 注释约束和 provider 参数注释约束。
- `docs/dev/code-style.md` 已包含必须写注释场景、导出 API 注释、参数数组执行和 TypeScript strict 约束。

已执行文件存在性与关键内容检查：

```powershell
Test-Path -LiteralPath 'AGENTS.md'
Test-Path -LiteralPath 'docs\progress\status.md'
Test-Path -LiteralPath 'docs\progress\timeline.md'
Test-Path -LiteralPath 'docs\progress\logs\2026-05-25.md'
Select-String -LiteralPath 'AGENTS.md' -Pattern 'Subagent 设置','docs/progress/status.md','docs/progress/timeline.md','docs/progress/logs/YYYY-MM-DD.md','rolemux-cli-builder','rolemux-skill-builder','rolemux-reviewer'
Get-ChildItem -Recurse -File -LiteralPath 'docs\progress'
```

验证结果：

- `AGENTS.md` 存在。
- `docs/progress/status.md` 存在。
- `docs/progress/timeline.md` 存在。
- `docs/progress/logs/2026-05-25.md` 存在。
- `AGENTS.md` 已包含 `Subagent 设置`、三类留痕文档路径、`rolemux-cli-builder`、`rolemux-skill-builder`、`rolemux-reviewer` 等关键内容。

## GitHub README 展示更新

本轮根据用户要求补充 GitHub 可展示的 README 内容。

变更内容：

- 将 `README.md` 从简要英文说明改为中文 GitHub 首页文档。
- 补充项目定位、当前状态、已实现功能、工作原理、环境要求、GitHub 分支安装、本地源码运行、初始化、常用命令、任务产物、配置、Skill 用法、开发验证、安全默认值和已知限制。
- 明确当前 npm 包尚未正式发布，推荐先从 `github:immortalbeating2/rolemux#feature/complete-rolemux-plugin` 或源码安装试用。
- 在 `package.json` 增加 `prepare` 脚本，使 GitHub 分支安装时可自动执行构建，生成 CLI 入口需要的 `dist/cli.js`。

验证命令：

```powershell
npm run typecheck
npm test
npm run build
npm pack --dry-run
git diff --check
Select-String -LiteralPath README.md -Pattern '从 GitHub 安装试用','当前状态','常用命令','任务产物','npm install -g github'
```

验证结果：

- `npm run typecheck` 通过。
- `npm test` 通过，11 个 test files / 17 个 tests。
- `npm run build` 通过。
- `npm pack --dry-run` 通过，触发 `prepare` 构建，包清单仍包含 `dist`、`skills`、`roles`、`templates`、`examples`、`docs/release/checklist.md`、`README.md` 和 `LICENSE`。
- `git diff --check` 通过。
- README 关键章节与 GitHub 安装命令均可检索。

阶段文档验证命令：

```powershell
(Get-ChildItem -LiteralPath 'spec\phases' -File | Measure-Object).Count
(Get-ChildItem -LiteralPath 'spec\implementation' -File | Measure-Object).Count
Select-String -LiteralPath 'AGENTS.md' -Pattern 'spec/phases/README.md','spec/implementation/README.md','m0-project-initialization-plan.md','阶段开发必须读取','Session 接续顺序','阶段推进、阶段退出标准'
$json = Get-Content -LiteralPath '.codex\agents\agents.json' -Raw | ConvertFrom-Json; $json.rootInstructions | Select-String -Pattern 'spec/phases','spec/implementation'; ($json.agents | Measure-Object).Count
rg -n "TBD|待验证|将在本轮最终检查后补充|当前尚未初始化 git" AGENTS.md spec docs .codex/agents --glob '!docs/progress/*'
```

阶段文档验证结果：

- `spec/phases/` 包含 8 个文件：README 与 M0-M6 七个阶段开发文档。
- `spec/implementation/` 包含 8 个文件：README 与 M0-M6 七个阶段实施文档。
- `AGENTS.md` 已包含阶段文档入口、实施文档入口、M0 实施文档、阶段读取门禁、Session 接续顺序和阶段任务类型读取规则。
- `.codex/agents/agents.json` 可解析，仍包含 6 个角色，并已增加阶段/实施文档读取指令。
- 排除进度日志后的占位符扫描未发现残留声明。
- 当前存在未跟踪文件 `AGENTS - 副本.md`，视为用户本地副本，本轮不修改、不提交。

## 风险与遗留问题

- npm 包尚未正式发布，当前推荐通过 GitHub 分支或源码方式试用。
- 后续真实 provider 参数需要通过 `doctor` 和 adapter 测试持续确认。
- Windows 下 mock `.cmd` provider 验证不稳定，后续需补跨平台 mock provider 执行策略。
