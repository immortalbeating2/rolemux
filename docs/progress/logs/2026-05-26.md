# 2026-05-26 开发日志

## 本轮目标

根据用户要求补充 RoleMux 卸载功能，形成安装生命周期闭环。

## 设计边界

- 新增 `rolemux uninstall`。
- 支持 `--dry-run`，只列出卸载目标，不删除文件。
- 支持 `--keep-config`，保留 `~/.rolemux/config.toml`。
- 默认只删除 RoleMux 明确安装的路径：
  - `~/.rolemux/config.toml`
  - `~/.rolemux/roles/`
  - `~/.codex/skills/rolemux-workflow/`
  - `~/.claude/skills/rolemux-workflow/`
- 不删除用户项目文件，不修改或删除 `AGENTS.md`。
- 不整目录删除 `~/.rolemux`，避免误删用户自定义文件。

## 实施内容

- 新增 `src/commands/uninstall.ts`。
- 在 `src/cli.ts` 注册 `uninstall` 命令。
- 在 `src/index.ts` 导出 `uninstallCommand`。
- 新增 `tests/commands/uninstall.test.ts`，覆盖 dry-run、真实卸载和 `--keep-config`。
- 更新 `tests/cli-smoke.test.ts`，确认 CLI 注册 install/uninstall 生命周期命令。
- 更新 `README.md`、`docs/release/checklist.md` 和 `spec/rolemux-development-spec.md`。
- 更新 `docs/progress/status.md` 和 `docs/progress/timeline.md`。

## TDD 记录

先写失败测试：

```powershell
npx vitest run tests/commands/uninstall.test.ts
npx vitest run tests/cli-smoke.test.ts
```

失败原因：

- `tests/commands/uninstall.test.ts` 无法导入 `src/commands/uninstall.js`。
- `tests/cli-smoke.test.ts` 显示命令表没有 `uninstall`。

实现后验证：

```powershell
npx vitest run tests/commands/uninstall.test.ts
npx vitest run tests/cli-smoke.test.ts
```

结果：

- `tests/commands/uninstall.test.ts` 通过，3 个测试。
- `tests/cli-smoke.test.ts` 通过，2 个测试。

## 风险与后续建议

- 当前卸载命令不删除 `~/.rolemux` 父目录，目的是避免误删用户自定义文件；README 已明确这一点。
- 后续若需要彻底删除空父目录，可新增显式选项，例如 `--prune-empty-dirs`，不要默认启用。

## 发布前 E2E 验收补充

根据用户确认，补充可重复的发布前端到端验收脚本。

变更内容：

- 新增 `tests/fixtures/mock-provider.mjs`，输出确定性的 `MOCK_PROVIDER_OUTPUT`。
- 新增 `tests/e2e/release-flow.test.ts`，通过构建后的 `dist/cli.js` 执行完整本地流程。
- 新增 provider command override：
  - `ROLEMUX_PROVIDER_<PROVIDER>_COMMAND`
  - `ROLEMUX_PROVIDER_<PROVIDER>_ARGS_PREFIX`
- 新增 `vitest.e2e.config.ts`，将 E2E 与常规单元测试隔离。
- 新增 npm scripts：
  - `npm run test:e2e`
  - `npm run verify:release`
- 更新 README、release checklist、mock provider 示例和产品 spec。

TDD 红灯：

```powershell
npx vitest run tests/providers/provider-adapters.test.ts
npm run build; npx vitest run tests/e2e/release-flow.test.ts
```

失败原因：

- provider adapter 不支持 executable/args-prefix override。
- E2E mock provider fixture 尚不存在。

转绿验证：

```powershell
npx vitest run tests/providers/provider-adapters.test.ts
npm run build; npx vitest run tests/e2e/release-flow.test.ts
npm run test:e2e
```

结果：

- provider adapter override 测试通过。
- E2E 发布流测试通过，覆盖 install、mock provider 非 dry-run run、task artifact、status、clean、uninstall。
- `npm run test:e2e` 通过，构建后执行 1 个 E2E 测试。

最终一键发布验收：

```powershell
npm run verify:release
```

结果：

- `npm run typecheck` 通过。
- `npm test` 通过，12 个 test files / 22 个 tests。
- `npm run test:e2e` 通过，1 个 E2E test。
- `npm pack --dry-run` 通过，包清单包含 `dist`、`skills`、`roles`、`templates`、`examples`、`docs/release/checklist.md`、`README.md` 和 `LICENSE`。
- `git diff --check` 通过。

## 最终验证

```powershell
npx vitest run tests/commands/uninstall.test.ts tests/cli-smoke.test.ts
npm run typecheck
npm test
npm run build
npm pack --dry-run
git diff --check
node .\dist\cli.js --help
node .\dist\cli.js uninstall --dry-run
```

结果：

- targeted tests 通过，2 个 test files / 5 个 tests。
- 全量测试通过，12 个 test files / 21 个 tests。
- typecheck、build、npm pack dry-run、git diff whitespace check 均通过。
- `node .\dist\cli.js --help` 已展示 `uninstall [options]`。
- `node .\dist\cli.js uninstall --dry-run` 只列出目标，`removed` 为空。
- 使用临时 HOME 执行 install -> uninstall 闭环后，config、roles、Codex Skill、Claude Skill 均被删除，自定义 `.rolemux/custom.md` 保留。
