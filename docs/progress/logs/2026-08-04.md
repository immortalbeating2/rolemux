# 2026-08-04 开发日志

## 目标

按 P0-P3 增强 RoleMux：执行前 provider 检查、可选深度 probe，以及 provider-native subagent 的共享 monitor/TUI 展示。

## 实现

- 新增共享 executable preflight，接入 `run`、`plan`、`review`、`discuss`、`dispatch`；失败返回结构化 `blocked`，不写任务产物或部分启动。
- 新增 `doctor --probe --probe-timeout-ms`，复用 adapter 与 runner，分类 `auth`、`network`、`output`、`process`、`timeout`。
- 新增 stdout JSONL line callback、原生事件 parser、嵌套 `nativeAgents[]` 与 TUI 展示。
- 新增 `dispatch --native-agents`；当前只允许真实认证通过的 Claude 与 Agy。child 共享父 provider 权限，不是独立 RoleMux worker。

## 真实认证

- Claude：`task_started` / `task_notification`，稳定 `task_id`，完成父输出 `PARENT_OK`。
- Agy：`step_update` 的 `subagent ACTIVE/DONE`，稳定 `conversation_id`，完成父输出 `PARENT_OK`。
- Codex：两次出现 `collab spawn failed`，未获得有效 child lifecycle。
- Grok：可见 `spawn_subagent` 工具，但真实调用失败；同时存在本机代理认证刷新告警。
- OpenCode：JSON 只在 child 完成后给出 `tool_use`，不满足实时开始/完成监控门槛。

## 验证与边界

- 完整 `npm run verify:release` 通过：35 个 test files / 165 项 unit tests、3 项 CLI E2E、typecheck、build、pack dry-run 与 `git diff --check`。
- `npm audit --omit=dev --audit-level=high` 为 0 vulnerabilities；构建后 `doctor` 与 `dispatch --help` smoke 通过。
- Codex App 插件源/cache、Claude junction 源和 `.agents-skills` 副本已刷新，5 份 Skill SHA-256 均为 `97E9CD4DBCC538470D394A8ECF311E9EB8EB739C06F676DEBCA8717B47202142`。
- 未新增依赖，未读取凭据，真实探针在临时目录只读运行。
- 不使用文本启发式猜测未认证 provider 的 child 状态。

## 顶层 fan-out 最小优化

- 真实 parent task `20260804T110456-d780c3` 中 Codex/Grok 均成功，但 monitor 丢失 Codex `startedAt`，并把 Grok terminal 推迟到 Codex 完成后。
- 回归测试先稳定复现并发 read-modify-write 覆盖和批量 terminal 延迟，再将快照更新收敛为单一事务队列，并在每个 provider 完成时立即发布 terminal 状态。
- 父/子任务产物完成后单独写 `agent-artifact`，不再改变 worker 的真实 `startedAt` / `finishedAt`。
- 普通 workflow 继续只做 executable preflight；Skill 仅在首次、配置/认证变化、失败/超时或认证不确定的高成本任务前调用深度 probe。
- 默认 fan-out 阈值为至少两个独立角色/子任务且单任务预计约 30 秒以上；显式用户要求仍可覆盖。
- 新增读取态 elapsed 回归：无 monitor 事件的 5 秒期间，`agents --json` / TUI 仍应显示递增时间，不额外写盘。
- 修复后真实 parent task `20260804T112650-5b7b1b`：Grok 17.232 秒时独立 success，Codex 64.916 秒完成；最终 monitor 与 subtask metadata 计时一致。
- 最终验证：聚焦 monitor/dispatch `26/26`，全量 unit `165/165`、E2E `3/3`，build、pack dry-run、`git diff --check` 和 runtime audit 全部通过。
- Codex/Claude Skill、Codex 插件源/cache、Claude junction 源与 `.agents-skills` 保留副本共 7 份哈希一致：`A4D2F073A8A0DB49AF732D42440B9AD02615A6D1723125F4D335CC3AE3F76068`。
