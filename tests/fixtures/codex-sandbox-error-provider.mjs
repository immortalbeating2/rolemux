#!/usr/bin/env node

console.log('I could not complete the read-only review because every local read path was blocked.');
console.log('windows sandbox: CryptUnprotectData failed: 2148073483');
process.exit(0);
